open Tailwind.Dsl
open Utils

let () = klass @@ break_inside auto
let () = klass @@ break_inside avoid
let () = klass @@ break_inside all
let () = klass @@ break_inside avoid_page
let () = klass @@ break_inside page
let () = klass @@ break_inside left
let () = klass @@ break_inside right
let () = klass @@ break_inside column
